export enum Resource {
  ROLE = "roles",
  USER = "users",
  JOB = "jobs",
  PLAYBOOK = "playbooks",
  COURSE = "courses",
  LESSON = "lessons",
  USE_CASE = "use-cases",
  APPLICATION_URL = "application-urls",
}

export enum CourseType {
  LESSON = "LESSON",
  QUIZ = "QUIZ",
  SECTION = "SECTION",
}
